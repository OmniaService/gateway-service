server:
  port: 12000
spring:
  output.ansi.enabled: 'ALWAYS'
  application:
    name: gatway-service
  cloud:
    config:
      uri: http://localhost:15000

zuul:
  ignoredServices: '*'
  routes:
    user-service: /users/**
    serviceId: users