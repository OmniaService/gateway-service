server:
  port: 12000

spring:
  profiles:
    active: local
    include: routes
  output.ansi.enabled: 'ALWAYS'
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://localhost:15000